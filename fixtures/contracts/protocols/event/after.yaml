asyncapi: 2.6.0
info:
  title: Orders
  version: 2.0.0
channels:
  orders.created:
    publish:
      message:
        $ref: "#/components/messages/OrderCreated"
components:
  messages:
    OrderCreated:
      name: OrderCreated
      payload:
        type: object
        required:
          - orderId
          - currency
        properties:
          orderId:
            type: integer
          currency:
            type: string
