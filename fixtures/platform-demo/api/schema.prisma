datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Order {
  id        String   @id @db.Uuid
  status    String
  createdAt DateTime @map("created_at")

  @@map("orders")
}
